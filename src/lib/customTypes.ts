// Re-exports for backwards compatibility — canonical types live in integrations/supabase/types.ts
export type { Destination, FlightResult, Trip, TripMember, Preference, Vote } from '@/integrations/supabase/types'
